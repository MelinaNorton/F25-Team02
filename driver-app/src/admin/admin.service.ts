import { Injectable } from '@nestjs/common';

@Injectable()
export class AdminService {
  findAll() {
    
  }

  findOne(id: number) {
    
  }

  update(id: number, ) {
    
  }

  remove(id: number) {
    
  }
}
